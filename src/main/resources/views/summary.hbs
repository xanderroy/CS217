<!DOCTYPE html>
<html>
<head>
    <title>Summary of Spending</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            text-align: center;
            padding: 50px;
        }
        .container {
            width: 60%;
            margin: auto;
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            background-color: #4CAF50;
            color: white;
            padding: 10px;
            border-radius: 5px;
        }
        .summary {
            text-align: left;
            margin-top: 20px;
            font-size: 18px;
        }
        .summary p {
            background-color: #f2f2f2;
            padding: 10px;
            border-radius: 5px;
        }
        .error {
            color: red;
            font-weight: bold;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>Summary of Spending</h1>
    {{#if summary}}
        <div class="summary">
            {{#each summary}}
                <p><strong>{{@key}}:</strong> £{{this}}</p>
            {{/each}}
        </div>
    {{else}}
        <p>No spending data available.</p>
    {{/if}}
</div>
</body>
</html>
