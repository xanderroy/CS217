<!DOCTYPE html>
<html>
<head>
    <title>User Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            text-align: center;
            padding: 50px;
        }
        .container {
            width: 60%;
            margin: auto;
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            background-color: #4CAF50;
            color: white;
            padding: 10px;
            border-radius: 5px;
        }
        .details {
            text-align: left;
            margin-top: 20px;
            font-size: 18px;
        }
        .details p {
            background-color: #f2f2f2;
            padding: 10px;
            border-radius: 5px;
        }
        a {
            background-color: #f2f2f2;
            padding: 10px;
            border-radius: 5px;
        }
        .error {
            color: red;
            font-weight: bold;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>User Details</h1>
    {{#if error}}
        <p class="error">{{error}}</p>
    {{else}}
        <div class="details">
            <p><strong>ID:</strong> {{id}}</p>
            <p><strong>Name:</strong> {{name}}</p>
            <p><strong>Balance:</strong> £{{balance}}</p>
            <p><strong>Round Up:</strong> {{roundup}}</p>
        </div>
    <a href="/bank/{{id}}/transactions">Transactions</a>
    {{/if}}
</div>
</body>
</html>
