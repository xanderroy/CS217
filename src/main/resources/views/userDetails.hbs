<!DOCTYPE html>
<html>
<head>
    <title>User Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            text-align: center;
            padding: 50px;
        }
        .container {
            width: 60%;
            margin: auto;
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            background-color: #4CAF50;
            color: white;
            padding: 10px;
            border-radius: 5px;
        }
        .details {
            text-align: left;
            margin-top: 20px;
            font-size: 18px;
        }
        .details p {
            background-color: #f2f2f2;
            padding: 10px;
            border-radius: 5px;
        }
        a {
            background-color: #f2f2f2;
            padding: 10px;
            border-radius: 5px;
            margin-top: 10px;
            display: inline-block;
        }
        .error {
            color: red;
            font-weight: bold;
        }
        button {
            width: 20%;
            padding: 10px;
            margin-top: 20px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }

        .logout {
            background-color: red !important;
        }
        .sidebar {
            width: 220px;
            background-color: #4CAF50;
            color: white;
            padding: 20px;
            height: 100vh;
            position: fixed;
            left: 0;
            top: 0;
        }
        .sidebar a {
            display: block;
            color: white;
            text-decoration: none;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            background-color: #388E3C;
        }
        .sidebar a:hover {
            background-color: #2E7D32;
        }
    </style>
</head>
<body>

<div class="sidebar">
    <h2>ScotBank</h2>
    <a href="/bank/{{id}}/transactions">Transactions</a>
    <a href="/bank/{{id}}/summary">Summary</a>
    {{#if roundup}}
        <a href="/bank/{{id}}/reclaim">Reclaim Roundups</a>
    {{/if}}
    <a class="logout" href="/bank/logout">Logout</a>
</div>

<div class="container">
    <button onclick="window.history.back()">Go Back</button>
    <h1>User Details</h1>
    {{#if error}}
        <p class="error">{{error}}</p>
    {{else}}
        <div class="details">
            <p><strong>ID:</strong> {{id}}</p>
            <p><strong>Name:</strong> {{name}}</p>
            <p><strong>Balance:</strong> £{{balance}}</p>
            <p><strong>Round Up:</strong> {{roundup}}</p>
            {{#if roundup}}<p><strong>Round Ups Pot:</strong> £{{rounduppot}}</p>{{/if}}
        </div>

    {{/if}}
</div>
</body>
</html>
