<!DOCTYPE html>
<html>
<head>
    <title>Accounts List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            display: flex;
        }

        /* Left Sidebar Styling */
        .sidebar {
            width: 220px;
            background-color: #4CAF50; /* Green background */
            color: white;
            padding: 20px;
            height: 100vh; /* Full height */
            position: fixed;
            left: 0;
            top: 0;
        }

        .sidebar a {
            display: block;
            color: white;
            text-decoration: none;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            background-color: #388E3C; /* Darker green for the button */
        }

        .sidebar a:hover {
            background-color: #2E7D32; /* Darker hover effect */
        }

        .sidebar .logout {
            background-color: red !important; /* Red logout button */
        }

        /* Main Content */
        .container {
            margin-left: 240px; /* Shift content right to make space for sidebar */
            padding: 20px;
            width: 100%;
            text-align: center;
        }

        h1 {
            background-color: #4CAF50;
            color: white;
            padding: 15px;
            border-radius: 5px;
            display: inline-block;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 10px;
            border: 1px solid #ddd;
        }

        th {
            background-color: #388E3C; /* Darker green for contrast */
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        tr:hover {
            background-color: #c8e6c9; /* Soft green hover effect */
        }

        a {
            text-decoration: none;
            color: #388E3C; /* Darker green for links */
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
<div class="sidebar">
    <a href="/bank/report">View Sanction Report</a>
    <a href="/bank/bigspenders">View Big Spenders</a>
    <a class="logout" href="/bank/logout">Logout</a>
</div>
<div class="container">
    <h1>Accounts List</h1>
    <table>
        <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Starting Balance</th>
            <th>Round Up</th>
        </tr>
        </thead>
        <tbody>
        {{#each accounts}}
            <tr>
                <td>{{id}}</td>
                <td><a href="/bank/{{id}}">{{name}}</a></td>
                <td>£{{balance}}</td>
                <td>{{roundup}}</td>
            </tr>
        {{/each}}
        </tbody>
    </table>
</div>

</body>
</html>
